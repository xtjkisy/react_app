import Content from "./Content";
import Menu_btn from "./Menu-btn";

function Header() {
    return (
        <header>
            < Menu_btn />
            < Content />
        </header>
    );
}
export default Header;